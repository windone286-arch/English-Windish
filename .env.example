# ==========================================================================
# 环境变量模板
#
# 用法：复制为 .env，填入真实值。
#   cp .env.example .env
#
# .env 已被 .gitignore 排除，**不会**进入版本库。
# 这个模板文件本身不含任何密钥，可以安全提交。
# ==========================================================================

# --------------------------------------------------------------------------
# 服务商选择
#
# 为什么分两个？因为 DeepSeek 没有视觉模型，图片识别必须用通义或智谱。
# 所以「看图」和「读文本」可以是两家不同的服务商，各取所长。
# --------------------------------------------------------------------------

# 负责图片识别：dashscope（通义千问）或 zhipu（智谱）
VISION_PROVIDER=dashscope

# 负责语法分析与单词精讲：deepseek
TEXT_PROVIDER=deepseek

# --------------------------------------------------------------------------
# API 密钥
#
# 只需填你实际使用的那一家的密钥，另一家可以留空。
# --------------------------------------------------------------------------

# 通义千问（阿里云百炼）密钥
DASHSCOPE_API_KEY=

# 智谱 AI 密钥（用通义时可不填）
ZHIPU_API_KEY=

# DeepSeek 密钥
DEEPSEEK_API_KEY=

# DeepSeek 接口地址。
# 留空用官方地址；如果用第三方中转站，在这里填中转地址。
DEEPSEEK_BASE_URL=

# --------------------------------------------------------------------------
# 模型名（一般不用改）
# --------------------------------------------------------------------------

VISION_MODEL=qwen-vl-max
TEXT_MODEL=deepseek-chat

# --------------------------------------------------------------------------
# 访问保护（部署到公网时才需要）
# --------------------------------------------------------------------------

# 访问密码。留空 = 完全关闭保护，任何人都能打开站点。
# ⚠️ 部署到公网时必须设置，否则你的 API 额度会对所有人敞开。
ACCESS_PASSWORD=

# 每日分析次数上限。0 或留空 = 不限制。
# 第二道锁：密码可以被转发，转发出去就收不回来，
# 而限额能保证「最坏情况下的损失」始终可估算。
DAILY_LIMIT=30

# --------------------------------------------------------------------------
# 调试
# --------------------------------------------------------------------------

# 1 = 输出详细日志；0 = 只输出警告
DEBUG=0
