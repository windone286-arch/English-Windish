# ==========================================================================
# 开发依赖
#
# 只有本地开发和跑测试需要，**部署时不必安装**。
# 安装方式：pip install -r requirements-dev.txt
#
# 它通过 -r 引入运行时依赖，所以开发环境装这一个文件就够了。
# ==========================================================================

-r requirements.txt

# 测试
pytest>=8.3.0

# 命令行输出美化
rich>=13.9.0

# 数据处理（项目 1 的数据分析环节会用到）
pandas>=2.2.0

# 图片处理（生成示例图、调试识别效果时用到）
pillow>=10.4.0
